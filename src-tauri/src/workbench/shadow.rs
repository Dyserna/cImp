//! Phase C — the shadow checkpoint repo (`ensure`/`snapshot`/`list`/
//! `diff_vs_now`/`restore`/`gc`) at `<root>/.cimp/shadow.git`, built on the
//! §0.2 git harness via [`super::git::GitCtx::shadow`]. Not yet implemented;
//! this file exists so `mod shadow;` resolves and the Phase A module layout
//! matches the milestone's final shape.
