//! Phase D — the worktree manager (`create`/`list`/`merge`/`discard`/
//! `prune`) at `<root>/.cimp/worktrees/<slug>`, built on the §0.2 git
//! harness. Not yet implemented; this file exists so `mod worktree;` resolves
//! and the Phase A module layout matches the milestone's final shape.
