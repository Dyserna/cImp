//! Phase B — unified-diff parser + live diff-pane types (`FileDiff`, `Hunk`,
//! `parse_unified`, `summary`/`diff_file` over the §0.2 git harness). Not yet
//! implemented; this file exists so `mod diff;` resolves and the Phase A
//! module layout matches the milestone's final shape.
